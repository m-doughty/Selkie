name = Selkie

[ReadmeFromPod]
filename = docs/Readme.rakudoc

[RunBeforeBuild]
; Regenerate the Pod-extracted API reference under docs/api/ before every
; build. Keeps the per-module docs in lock-step with lib/ source. %x is
; the raku executable mi6 is using.
; cmd = %x tools/build-api-docs.raku

[UploadToZef]

[AutoScanPackages]
enabled = false

[Badges]
provider = github-actions/test.yml
